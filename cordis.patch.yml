# One top-level loader row only. The package itself composes the host/client
# halves of @linxin666/dsh-ssh, so the profile does not need (and must not try)
# to resolve @linxin666/dsh-ssh as a second top-level dependency.
- insert:
    - id: ssh-files-sidebar
      name: 'dsh-ssh-files-sidebar'
